# WORKFLOW

이 레포의 모든 작업은 아래 규칙을 따른다. **예외 없이 적용한다.**

## 1. 계획 우선 (Plan First) — 필수

**어떤 구현도 계획 없이 시작하지 않는다.**

- 새 기능/변경을 시작하기 전에 **반드시 `docs/plan/` 아래에 계획 문서를 먼저 작성**한다.
- **계획 수립 시 `omc-plan`(`/oh-my-claudecode:plan`)을 사용한다.** 즉흥적으로 직접 작성하지 말고, omc-plan으로 계획을 도출한 뒤 그 결과를 `docs/plan/` 문서로 정리한다.
- 계획 문서는 다음을 포함한다: 목표, 모듈/파일 구조, 데이터 흐름, 핵심 설계 결정과 이유, 비용/리스크 고려사항, 검증 방법, 작업 순서.
- 계획을 사람이 검토·승인한 뒤에 구현에 들어간다. (문서 상단 `Status` 갱신)
- 구현 중 계획이 바뀌면 코드보다 **계획 문서를 먼저 갱신**한다.
- **외부 데이터·시세 API에 의존하는 작업은 계획을 확정하기 전에 스파이크(throwaway 실측)로 범위·인증·데이터 품질을 검증**한다. 가짜 가정 위에 계획·구현하지 않기 위함이다. (실제로 FDR 기간 한계·pykrx의 KRX 로그인 요구·데이터 결측 버그를 구현 전에 스파이크로 잡아냈다.)

### 계획 문서 규칙
- 경로: `docs/plan/`
- 파일명: `YYYY-MM-DD-<short-slug>.md` (예: `2026-06-02-daily-backtest.md`)
- 상단에 `Status: 제안됨 | 승인됨 | 진행중 | 완료 | 보류` 를 명시한다.
- **계획 문서는 처음부터 워크트리(브랜치) 안에서 작성·커밋**한다. `main` 작업트리에 untracked 초안을 남기지 않는다(병합 후 `git pull`이 같은 경로 충돌로 막힌다).

## 2. 브랜치 + 워크트리에서만 작업 — 필수

**`main`에서 직접 커밋하지 않는다.**

- 모든 작업은 `main`에서 분기한 **브랜치 + git worktree** 안에서 수행한다.
- 워크트리 생성:
  ```bash
  git worktree add ../worktrees/<repo>--<slug> -b <type>/<slug> main
  ```
- 작업 완료 후 PR로 `main`에 병합한다. 병합 후 워크트리는 정리한다:
  ```bash
  git worktree remove <path>
  git branch -d <branch>
  ```

### 브랜치 네이밍 규칙

형식: **`<type>/<slug>`** (선택적으로 이슈 번호: `<type>/<issue>-<slug>`)

**type (목적별 접두사)**

| type | 용도 |
|------|------|
| `feature/` | 새 기능 추가 |
| `fix/` | 버그 수정 |
| `chore/` | 빌드·설정·의존성 등 잡무 |
| `docs/` | 문서 변경 |
| `refactor/` | 동작 변경 없는 구조 개선 |
| `test/` | 테스트 추가·수정 |
| `exp/` | 실험·전략 리서치 (병합 안 할 수도 있음) |

**slug 규칙**
- 영어 **kebab-case** (소문자 + 하이픈). 예: `daily-backtest`, `kis-data-collector`
- 간결하게 2~4단어. 무엇을 하는지 한눈에 드러나게.
- 공백·대문자·언더스코어·한글 금지.

**예시**
- `feature/daily-backtest`
- `feature/kis-realtime-collector`
- `fix/lookahead-bias-in-engine`
- `exp/rsi2-threshold-sweep`
- `docs/12-update-workflow` (이슈 #12)

**금지**
- `main`, `master`, `develop` 등 보호/공용 브랜치명 재사용 금지
- `temp`, `test1`, `mybranch`처럼 목적 불명한 이름 금지

## 3. 완료 전 검증 — 필수

- 완료를 선언하기 전에 테스트/실행으로 동작을 검증하고 증거를 남긴다.
- "됐을 것이다"가 아니라 "실행해서 확인했다"로 보고한다.

## 4. 커밋

- 작은 단위로 자주 커밋한다. 메시지는 무엇을/왜를 명확히.
- 비밀키·`.env`·데이터 파일은 커밋하지 않는다 (`.gitignore` 준수).
- 새 외부 서비스 자격증명이 필요하면 **`.env.example`에 빈 플레이스홀더로 키 이름만 문서화**하고, 실제 값은 `.env`(gitignore)에만 둔다.

## 5. 백테스트 무결성 — 필수

전략 백테스트는 결과를 신뢰하기 전에 다음을 항상 점검한다. **어느 하나라도 빠지면 결과가 부풀려진다.**

- **체결 룩어헤드 차단**: 시그널은 종가로 계산하되 체결은 다음 봉 시가(next-bar-open).
- **선택 룩어헤드·생존편향 차단**: 종목 유니버스는 "현재" 기준이 아니라 **시점별**로 구성하고, 상폐 종목도 상장돼 있던 기간엔 포함한다.
- **수정주가 점검**: 액면분할·권리락 미반영 가격 점프가 가짜 시그널을 만든다 — 스캐너로 플래그한다.
- **현실적 비용**: 수수료·슬리피지·세금(개별주 매도세 등)을 반영한다.
- **벤치마크 대비**: 단순보유(buy&hold)·무작위 대비 우위가 있는지 본다. 절대 수치만으로 "통한다"고 하지 않는다.
- **정직한 음의 결과**: "엣지 없음"도 값진 결과다. 표본만 보고 낙관하지 않는다.

## 6. 전략 실패 시 기록 — 필수

**전략이 실패하면(엣지 없음·가설 기각·중단) 반드시 `docs/failure/` 아래에 실패 문서를 작성한다.**

- 같은 실패를 반복하지 않기 위함이다. "안 통한다를 싸게 확인한 것"도 자산이므로 근거와 함께 남긴다.
- 파일명: `YYYY-MM-DD-<short-slug>.md` (예: `2026-06-03-pead-large-cap.md`)
- 문서는 다음을 포함한다: 판정(한 줄 요약), 무엇을 어떻게 검증했나(결과 표), **실패 원인(영향 큰 순)**, 이 실패가 증명하지 *않는* 것(범위 한정), 건진 자산(재사용 가능 코드), 공정한 재시도에 필요한 것, 교훈.
- 관련 계획 문서(`docs/plan/`) 상단 `Status`를 `종료/보류`로 갱신하고 실패 문서를 링크한다.

---

요약: **계획 문서 작성(외부 의존은 스파이크 선검증) → 승인 → main에서 브랜치+워크트리 생성 → 거기서만 구현 → 검증(백테스트는 무결성 점검) → PR 병합. 실패하면 `docs/failure/`에 원인을 기록한다.**
